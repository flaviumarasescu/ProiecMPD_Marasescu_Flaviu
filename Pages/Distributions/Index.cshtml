@page
@model ProiecMPD_Marasescu_Flaviu.Pages.Distributions.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Distributions</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Distribution[0].DistributionName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Distribution) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.DistributionName)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.ID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.ID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.ID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
